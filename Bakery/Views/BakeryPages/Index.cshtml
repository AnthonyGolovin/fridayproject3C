@using Bakery.Models;

<h1>Pierres Bakery:</h1>
@if (Model.Count == 0)
{
  <p>You have no vendors! So sad :(</p>
}
<ul>
  @foreach (Vendor vendor in Model)
  {
    <li><a href='/bakerypages/@vendor.Id'>@vendor.CityName</a></li>
  }
</ul>

<a href="/bakerypages/new">Add a vendor</a>

<form action="/bakerypages/delete" method="post">
  <button type="submit" name="button">Clear vendor list</button>
</form>